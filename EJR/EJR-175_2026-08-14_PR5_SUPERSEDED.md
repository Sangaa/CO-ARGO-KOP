# EJR-175 — PR #5 Superseded

PR #5 is superseded because its CI merge snapshot was based on `6abfd40...` while current `main` advanced to `598a523...` during the revalidation session.

The two integration failures were diagnosed as:

- stale CORE-000 formatting test contract;
- stale REP-013 merge snapshot.

No merge occurred. No Runtime defect was established by PR #5 CI.

State: **SUPERSEDED / NOT MERGED**.
