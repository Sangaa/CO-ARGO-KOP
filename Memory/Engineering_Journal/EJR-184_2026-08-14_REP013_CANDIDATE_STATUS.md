# EJR-184 — REP-013 Candidate Status

The next mutation must start from the current `main` checkpoint and explicitly refresh REP-013 so the merge snapshot contains the canonical specification path.

Integrity remains HOLD.
