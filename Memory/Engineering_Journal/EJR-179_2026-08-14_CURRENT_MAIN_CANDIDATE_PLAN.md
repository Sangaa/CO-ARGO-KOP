# EJR-179 — Current Main Candidate Plan

This checkpoint defines the next controlled validation candidate after PR #6 was proven stale against the evolving `main` branch.

Candidate requirements:

- base directly on latest `main`;
- retain the Runtime authorization semantics correction;
- retain the corrected CORE-000 integration contract;
- make no unrelated Integration, CORE, REP-013, or baseline-authority changes;
- run fresh prototype + integration CI;
- record every PASS, FAIL, PARTIAL, and NOT_PERFORMED result in the REP-020 evidence stream.

Integrity remains `HOLD` until the required gates are satisfied.
